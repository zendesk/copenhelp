<ul title="Services" class="categories btn-group">
  {{#each categories}}
    <li class="unselectable">
      <button role="link" class="category btn" data-value="{{key}}" aria-label="Look for {{title}} services">
        <i class="category-icon icon-{{icon}}"></i>{{title}}
      </button>
    </li>
  {{/each}}
</ul>
